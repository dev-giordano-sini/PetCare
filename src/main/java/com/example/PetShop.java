package com.example;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.ArrayList;
import java.util.List;

public class PetShop {

    /*List<Pet> pets = new ArrayList<>();

    public void registerPet(String name, String breed, String ownerName, String contactInfo, String registrationDateStr) {
        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm");
        LocalDateTime registrationDate = LocalDateTime.parse(registrationDateStr, formatter);

        Pet pet = new Pet(name, breed, ownerName, contactInfo, registrationDate);
        pets.add(pet);
    }

    public void scheduleAppointment(Pet pet, AppointmentType type, String dateTimeStr, String note) {
        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm");
        LocalDateTime dateTime = LocalDateTime.parse(dateTimeStr, formatter);

        pet.setAppointment(new Appointment(type, dateTime, note));
    }*/
}
